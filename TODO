# Modify firenvim config so it has
	- smaller font
	- minimal UI

# Set default tab length to 4 spaces(?)
# default to spaces

# Move LPS config out of init
# Move Telescope config out of init
# Install Harpoon
#
